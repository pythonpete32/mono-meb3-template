import SimpleStorageComponent from "../../components/SimpleStorage";

export default function Page(): JSX.Element {
  return (
    <main className="flex flex-col items-center pt-60 min-h-screen">
      <SimpleStorageComponent />
    </main>
  );
}
